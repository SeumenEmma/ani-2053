## Tests

Le workspace contient 60 suites de tests avec la commande si :

```
jenga test --project NKMath_Tests --force
```
Dans `Nkentseu.jenga`, la configuration des tests est :

```python
dutc(enable=True)
dute(enable=True)
```

Suites existantes : 60
Suites exécutées : 1 (NKMath_Tests)
Tests exécutés : 8
Tests réussis : 8/8
Assertions réussies : 499/499
Taux de réussite : 100 %